# Chinmaya Vidyalaya Tarapur

## Run

- Install dependencies with `npm install`.
- Start the development preview with `npm run dev -- --port 5000`.
- Create a production build with `npm run build`.

This is a React 19 + TypeScript + Vite website. The home hero scrubs the imported 1280×720 frame sequence in `public/hero-frames`.